<h1>My Bucketlist</h1>

<p>Things I'm going to do before I die</p>

<div id="map-canvas">

</div>

<p>
  <%= link_to 'Sort By Name', destinations_path(:order => :name), :remote => true %><br />
  <%= link_to 'Sort By Name Desc', destinations_path(:order => 'name desc'), :remote => true %>
</p>

<div id="destinations">
  <% @destinations.each do |d| %>
    <%= render d %>
  <% end %>
</div>
